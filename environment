environment.py should be a file name. It has to be created more than one Testcase.

before_all(context) and after_all(context) - These run before and after the whole execution in testcase.
These methods can be used only in environment.py file.
